{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}

    <h1 class="ml-3">User</h1>

    <table class="table">
        <tbody class="text-white" >
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Nom de l'utilisateur</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
            <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>
    <form method="POST" action="{{ path('admin_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Fais pas ta tepu laisse lui une chance');">
    <button class="btn btn-danger ml-3">Supprimer</button>
</form>
<form method="GET" action="{{ path('admin_user_index') }}">
    <button class="btn btn-info m-3">Retour à la liste</button>
</form>
  

 
{% endblock %}